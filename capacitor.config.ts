import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'documentum_dash',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
